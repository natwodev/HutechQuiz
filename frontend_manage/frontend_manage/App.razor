@code {
    [Microsoft.AspNetCore.Components.RouteAttribute("/")]
    public partial class RedirectToLogin : Microsoft.AspNetCore.Components.ComponentBase
    {
        [Inject] private NavigationManager Navigation { get; set; }
        protected override void OnInitialized()
        {
            // Only redirect from root, and avoid force reloading the whole app
            var current = Navigation.Uri;
            if (!current.EndsWith("/student-login", StringComparison.OrdinalIgnoreCase))
            {
                Navigation.NavigateTo("/student-login", replace: true);
            }
        }
    }
}

<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
<<<<<<< HEAD
<RedirectToLogin />

=======

 
>>>>>>> main
